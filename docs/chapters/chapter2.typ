// ============================================================
// 第二章 相关技术理论
// ============================================================

= 相关技术理论

在开始介绍系统设计之前，有必要先说明项目涉及的技术。本章将简洁地介绍这些技术在项目中的应用。

== 物联网通信技术

=== MQTT 协议

MQTT是本项目用于硬件数据上报的协议。MQTT由IBM于1999年提出，2014年成为OASIS标准@mqtt_spec，是目前物联网领域应用最广泛的消息传输协议之一。选择该协议的原因如下。

==== 为什么选MQTT

+ *轻量级*：报文头部最小只有2字节，对带宽要求低。ESP32通过WiFi发送数据，每次只有几十字节，MQTT的开销几乎可以忽略。
+ *发布/订阅模式*：硬件端作为消息发布方，无需关注具体订阅方；后端通过订阅对应主题接收数据，实现发布/订阅解耦。
+ *QoS支持*：可以设置消息质量级别。本系统采用QoS 1级别，保证消息至少送达一次，但允许少量重复（重复消息在后端做去重处理）。

==== 实际应用中的问题

MQTT的QoS 1存在消息重复送达的可能性。当前原型中该问题出现概率较低，若后续生产部署需保证数据严格一致，可在业务逻辑中添加去重处理：每次更新座位状态前，先检查消息时间戳或序列号是否比数据库记录更新。

MQTT消息采用JSON格式，包含`seatId`、`timestamp`和`status`字段。硬件上报FREE（空闲）、OCCUPIED（有人）两种状态，后端合并预约状态后推送给前端。

=== WebSocket 与 Socket.io

WebSocket用于后端向前端推送状态更新。WebSocket协议在RFC 6455中定义，提供了浏览器与服务器之间的全双工通信通道，相比HTTP轮询显著降低了延迟和服务器负载。

==== 选择Socket.io而非原生WebSocket的原因

初期采用原生WebSocket，但遇到以下问题：

+ *断线重连需自行实现*：WiFi网络不稳定时，WebSocket连接经常断开，需要编写重连逻辑。
+ *心跳机制需自行实现*：需定期发送ping消息保持连接，否则会被服务器超时关闭。
+ *浏览器兼容性*：部分老版本浏览器不支持WebSocket，需做降级处理。

更换为Socket.io后，上述问题得到解决：

+ *自动重连*：连接断开后自动尝试重连，无需手动实现。
+ *心跳保活*：内置心跳机制，定期发送ping/pong。
+ *降级兼容*：不支持WebSocket时，自动降级到长轮询。
+ *房间机制*：本项目使用最多的功能，可将用户按区域分组。

通过Socket.io的房间机制，将用户按图书馆区域分组，状态更新仅推送给目标区域内的用户，避免全局广播带来的性能开销。

== 后端技术栈

=== NestJS 框架

后端框架在Java与Node.js之间进行选型对比，最终选用Node.js生态中的NestJS框架。NestJS @nestjs_docs 是一个用于构建高效、可扩展的服务器端应用程序的框架，采用模块化架构和依赖注入设计模式，深受Angular启发。

==== NestJS与Express的技术对比

Express是Node.js最流行的Web框架，但缺乏官方项目结构规范和TypeScript原生支持。NestJS在此基础上提供了：

#figure(
  table(
    columns: (1.2fr, 1.5fr, 1.5fr),
    align: center + horizon,
    table.header(
      [考量因素], [NestJS], [Express]
    ),
    [架构规范], [模块化+依赖注入，结构清晰], [自由灵活，需自行设计],
    [TypeScript支持], [原生支持，类型安全], [需额外配置],
    [WebSocket集成], [原生Gateway支持], [需额外库],
    [ORM集成], [与TypeORM深度集成], [需自行封装],
    [可测试性], [依赖注入便于Mock测试], [需自行处理依赖],
  ),
  caption: "NestJS与Express技术对比",
) <tbl:nestjs-express>

本系统的业务特点是：大量WebSocket长连接、频繁的MQTT消息处理、I/O密集型。NestJS的事件驱动模型和异步支持恰好契合上述场景。同时NestJS与TypeORM深度集成，通过装饰器即可定义实体关系，开发效率高。

==== 为什么选择NestJS

+ *功能满足*：模块化架构、依赖注入、拦截器、管道、守卫等机制完全满足复杂业务需求。
+ *开发效率*：基于已有的NestJS开发实践经验，TypeScript类型提示减少运行时错误。
+ *生态丰富*：与TypeORM、Redis、MQTT等库集成便利，中间件资源丰富。

=== MySQL 数据库

数据库选用MySQL 8.0，这是目前最主流的开源关系型数据库之一。

==== 选择MySQL的理由

+ *成熟稳定*：MySQL经过数十年生产环境验证，社区资料丰富，运维工具完善。
+ *性能优秀*：InnoDB存储引擎支持行级锁和MVCC，适合高并发读写场景。
+ *TypeORM支持*：TypeORM对MySQL的支持非常完善，自动生成迁移脚本。
+ *部署便利*：Docker一键部署，学校实验室环境易于搭建。

=== TypeORM

ORM层选用TypeORM @typeorm_docs，它是目前TypeScript生态中最成熟的ORM框架之一，与NestJS深度集成。

==== 为什么选TypeORM

TypeORM的核心优势是装饰器驱动的实体定义：通过`@Entity`、`@Column`、`@ManyToOne`等装饰器即可定义数据模型，自动生成数据库表结构和TypeScript类型。同时TypeORM内置参数化查询，自动防护SQL注入攻击；支持迁移脚本管理，便于数据库版本控制。

=== Redis

Redis @redis_docs 在本系统中承担三个角色：分布式缓存、原子计数器和分布式锁存储。

==== Redis的应用场景

+ *分布式锁*：座位预约采用Redis+Lua原子脚本实现，保证高并发下的互斥性。
+ *签到计数*：远程签到次数限制通过Redis计数器实现，支持过期时间自动重置。
+ *会话缓存*：用户登录态和设备指纹的缓存查询，降低数据库压力。

== 前端技术栈

=== React 18

前端框架基于已有的项目实践经验，选用React @react_docs 作为开发框架。React 18的并发渲染特性对座位状态频繁更新的场景具有良好支持。

=== Taro 跨端框架

小程序端采用Taro 4框架@taro_docs，一套代码可编译为微信小程序和H5，约80%代码可复用。平台差异部分（如登录API）需分别处理。

=== 轻量状态管理

小程序端采用轻量全局store（基于Zustand原理自实现）和页面hooks管理状态，避免引入过重封装，控制包体积在小程序限制范围内。

== 硬件技术

本系统的硬件层负责座位状态的实时检测和数据上报，由红外传感器和微控制器两部分组成。

=== 红外人体传感技术

本系统采用HC-SR501红外传感器检测座位状态。该传感器基于热释电效应，通过检测人体辐射的红外线（波长约10μm）判断是否有人。与HC-SR602（约8元）和毫米波雷达（50--100元）相比，HC-SR501（约4.35元）性价比最高，满足系统需求。传感器输出连接ESP32的GPIO引脚，检测到人体时输出高电平（3.3V），无人时输出低电平（0V）。

=== ESP32 微控制器

本系统采用ESP32-38Pin-CP2102作为主控芯片。ESP32 @esp32_datasheet 是乐鑫公司推出的高性能WiFi+蓝牙双模芯片，集成双核Xtensa LX6处理器，主频可达240MHz，内置520KB SRAM和4MB Flash。与STM32+ESP8266组合方案相比，ESP32单芯片方案的优势在于：

+ *集成度高*：WiFi、蓝牙、MCU集成在同一芯片，无需额外通信模块，PCB布线更简洁。
+ *性能更强*：双核处理器可同时处理传感器读取和网络通信任务。
+ *成本可控*：ESP32-38Pin-CP2102模块单价约¥25，与STM32+ESP8266组合成本相当，但省去了串口接线和模块间通信的开发调试工作。

ESP32负责读取红外传感器状态、进行时间窗口置信度计算，并通过MQTT协议直接将数据上报到后端。

== 本章小结

本章介绍了系统涉及的核心技术并进行了选型对比。通信技术方面，MQTT用于硬件数据上报，Socket.io实现前端实时推送。后端采用NestJS + TypeORM + MySQL + Redis，利用Redis Lua脚本实现分布式原子锁，通过参数化查询防护SQL注入。前端采用React 18 + Taro跨端框架。硬件层选用HC-SR501红外传感器和ESP32-38Pin-CP2102单芯片方案，兼顾成本与稳定性。技术选型遵循满足功能需求、控制开发成本、保证可维护性的原则。
